### Single stream UDP packet, 64B ###
#####################################
- name: udp_64B
  stream:
    self_start: True
    packet:
      binary: cap2/udp_64B_1pkt.pcap
    mode:
      type: continuous
      pps: 100
    rx_stats: []