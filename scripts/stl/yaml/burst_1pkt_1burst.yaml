### Single stream UDP packet, 64B ###
#####################################
- name: stream0
  stream:
    self_start: True
    packet:
      pcap: udp_64B_no_crc.pcap
    mode:
      type: single_burst
      pps: 100
      total_pkts : 100
